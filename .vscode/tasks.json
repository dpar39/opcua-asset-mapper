{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "ng serve",
            "type": "shell",
            "command": "npm start",
            "options": {
                "cwd": "${workspaceFolder}/ui"
            },
            "group": "build"
        }
    ]
}